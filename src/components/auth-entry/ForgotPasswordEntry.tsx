import { useState, type FormEvent } from "react";
import { useAuth } from "../../auth";
import { authEntryRepository } from "../../repositories/AuthEntryRepository";
export function ForgotPasswordEntry() { const auth = useAuth(); const [email, setEmail] = useState(""); const [sent, setSent] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); await auth.requestPasswordReset(email); authEntryRepository.markPasswordResetRequested(email); setSent(true); } return <main className="screen public-beta-screen"><section className="hero-card"><p className="pill">Password reset</p><h1>Request password reset metadata.</h1><p>Mock mode records reset metadata. Provider mode can send a real email when configured.</p></section><section className="panel"><form onSubmit={submit} className="stack"><input placeholder="Email" value={email} onChange={(e) => setEmail(e.target.value)} /><button className="primary-button">Send reset instructions</button></form>{sent && <p>Reset metadata recorded. Continue to <a href="/auth/reset-password?token=mock-reset">reset password</a>.</p>}</section></main>; }
