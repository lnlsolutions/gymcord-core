import { authEntryRepository } from "../../repositories/AuthEntryRepository";
export function ResetPasswordEntry() { const meta = authEntryRepository.getPasswordResetMetadata(); return <main className="screen public-beta-screen"><section className="hero-card"><p className="pill">Reset password</p><h1>Password reset metadata</h1><p>Token present: {String(meta.tokenPresent)}. This screen is provider-ready without bypassing auth.</p></section><section className="panel"><input placeholder="New password" type="password" /><button className="primary-button" onClick={() => { window.location.href = "/auth/login"; }}>Save new password metadata</button><pre>{JSON.stringify(meta, null, 2)}</pre></section></main>; }
