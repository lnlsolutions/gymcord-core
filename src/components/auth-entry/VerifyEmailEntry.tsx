import { authEntryRepository } from "../../repositories/AuthEntryRepository";
export function VerifyEmailEntry() { const meta = authEntryRepository.getEmailVerificationMetadata(); return <main className="screen public-beta-screen"><section className="hero-card"><p className="pill">Email verification</p><h1>Email verification metadata</h1><p>Status: {meta.status}. Mock mode is marked verified; provider mode can await a provider callback.</p></section><section className="panel"><pre>{JSON.stringify(meta, null, 2)}</pre><a className="primary-button" href="/auth/login">Continue to login</a></section></main>; }
