import type { Page } from "../../types/gymcord";export function MobileBottomNavigation({page,setPage,onMenu}:{page:Page;setPage:(p:Page)=>void;onMenu:()=>void}){const items:[Page,string,string][]=[["home","Home","🏠"],["train","Workouts","🏋️"],["meals","Journal","🍽️"],["coach","Atlas","🤖"]];return <nav className="bottom-nav" aria-label="Primary mobile navigation">{items.map(([id,label,icon])=><button key={id} className={page===id?"active":""} onClick={()=>setPage(id)} aria-current={page===id?"page":undefined}><span>{icon}</span><small>{label}</small></button>)}<button onClick={onMenu} aria-label="Open menu"><span>☰</span><small>Menu</small></button></nav>}
