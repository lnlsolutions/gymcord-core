import type { AppNotification } from "../../types/domain";
export function NotificationDetail({ notification }: { notification?: AppNotification | null }) { if (!notification) return <section className="panel"><h2>Selected notification</h2><p>Choose a notification to inspect delivery metadata.</p></section>; return <section className="panel"><h2>{notification.title}</h2><p>{notification.body}</p><div className="grid"><div className="card"><p>Status</p><strong>{notification.lifecycleStatus}</strong></div><div className="card"><p>Source</p><strong>{notification.sourceModule ?? "system"}</strong></div></div><pre>{JSON.stringify(notification.actions, null, 2)}</pre></section>; }
