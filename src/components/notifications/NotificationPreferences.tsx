import type { NotificationPreference } from "../../types/domain";
export function NotificationPreferences({ preferences, onChange }: { preferences: NotificationPreference; onChange: (preferences: NotificationPreference) => void; }) { return <section className="panel"><h2>Notification preferences</h2>{Object.entries(preferences.channels).map(([channel, enabled]) => <label className="exercise-row" key={channel}><input type="checkbox" checked={enabled} onChange={() => onChange({ ...preferences, channels: { ...preferences.channels, [channel]: !enabled } })} /><span>{channel}</span></label>)}<p>Digest: {preferences.digestFrequency}</p></section>; }
