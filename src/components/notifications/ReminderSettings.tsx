import type { NotificationPreference } from "../../types/domain";
export function ReminderSettings({ preferences, onChange }: { preferences: NotificationPreference; onChange: (preferences: NotificationPreference) => void; }) { const keys = ["workout", "nutrition", "progress", "calendar"] as const; return <section className="panel"><h2>Reminder settings</h2>{keys.map((key) => <label className="exercise-row" key={key}><input type="checkbox" checked={preferences.reminders[key]} onChange={() => onChange({ ...preferences, reminders: { ...preferences.reminders, [key]: !preferences.reminders[key] } })} /><span>{key} reminders</span></label>)}<p>Quiet hours: {preferences.reminders.quietHoursStart}–{preferences.reminders.quietHoursEnd}</p></section>; }
