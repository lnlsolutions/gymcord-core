import type { PaymentCustomerMetadata } from "../../repositories/PaymentRepository";
import type { SubscriptionMetadata } from "../../repositories/SubscriptionRepository";
export function PaymentStatusPanel({ customer, subscription }: { customer: PaymentCustomerMetadata; subscription: SubscriptionMetadata }) { return <section className="panel"><p className="eyebrow">Billing status metadata</p><h2>{subscription.status}</h2><div className="grid"><div className="card"><p>Customer</p><strong>{customer.audience}</strong><small>{customer.metadata.dataOwnership}</small></div><div className="card"><p>Invoice</p><strong>{subscription.invoice.status}</strong><small>{subscription.invoice.metadata.invoiceCreation}</small></div><div className="card"><p>Failed payment</p><strong>{subscription.failedPayment.retryCount} retries</strong><small>{subscription.failedPayment.metadata.dunningWorkflow}</small></div><div className="card"><p>Cancellation</p><strong>{subscription.cancellation.cancelAtPeriodEnd ? "scheduled" : "none"}</strong><small>{subscription.cancellation.metadata.hardDelete}</small></div></div></section>; }
