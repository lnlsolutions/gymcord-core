import type { QADiagnosticSnapshot } from "../../repositories/QARepository";
export function DeploymentReadinessScore({ snapshot }: { snapshot: QADiagnosticSnapshot }) { const label = snapshot.readinessScore >= 90 ? "Beta deployable" : snapshot.readinessScore >= 75 ? "Deploy with watchlist" : "Blocked for beta"; return <section className="shell-panel qa-score"><p className="eyebrow">Deployment readiness score</p><h2>{snapshot.readinessScore}%</h2><strong>{label}</strong><p>Provider: {snapshot.activeProvider} · Environment: {snapshot.environment} · Generated: {snapshot.generatedAt}</p></section>; }
