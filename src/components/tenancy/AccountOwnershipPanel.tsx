import { Card, Rows, type TenancyPanelProps } from "./TenancyShared";
export function AccountOwnershipPanel({ snapshot }: TenancyPanelProps) { const o = snapshot.ownership; return <Card title="Account ownership model"><Rows rows={[{ label: "Account", value: o.permanence, detail: "User account is permanent." }, { label: "Personal data owner", value: o.personalDataOwner, detail: "Gyms/trainers own access relationships only." }, { label: "Consumer-only", value: o.consumerOnlyAllowed ? "allowed" : "blocked" }, { label: "Leaving tenants", value: o.canLeaveRelationshipsWithoutDataLoss ? "keeps personal history" : "restricted" }, { label: "Export", value: o.exportReady ? "ready" : "pending" }]} /></Card>; }
