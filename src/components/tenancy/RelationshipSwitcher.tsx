import { Card, Rows, type TenancyPanelProps } from "./TenancyShared";
export function RelationshipSwitcher({ snapshot, onSwitchTenant, onSwitchTrainer }: TenancyPanelProps) { return <Card title="Relationship switcher"><Rows rows={[{ label: "Active tenant context", value: snapshot.activeTenantContextId ?? "consumer default" }, { label: "Active trainer context", value: snapshot.activeTrainerContextId ?? "none" }]} /><div className="dev-actions">{snapshot.relationships.map((item) => <button key={item.id} type="button" onClick={() => item.kind === "trainer" ? onSwitchTrainer?.(item.id) : onSwitchTenant?.(item.id)}>Switch to {item.displayName}</button>)}</div></Card>; }
