import { findWorkoutTemplate } from "../../data/workouts/workoutTemplates";
export function WorkoutDetail({id}:{id:string}){const w=findWorkoutTemplate(id);if(!w)return <section className="card"><h2>Workout not found</h2><a href="/workouts/library">Back to library</a></section>;return <section className="card"><p className="eyebrow">{w.trainingDay}</p><h2>{w.title}</h2><p>{w.level} • {w.durationMinutes} minutes • {w.equipment.join(", ")}</p><a className="primary" href={`/workouts/${w.id}/start`}>Start workout</a>{w.exercises.map(e=><article className="card" key={e.id}><h3>{e.name}</h3><p>{e.sets} sets • {e.reps ?? e.time} • Rest {e.restTime}</p><p>{e.formCues.join(" • ")}</p><small>Safety: {e.safetyNotes.join(" ")}</small></article>)}</section>}
