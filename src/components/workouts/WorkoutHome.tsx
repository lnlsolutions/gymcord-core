import { workoutTemplates } from "../../data/workouts/workoutTemplates";
export function WorkoutHome(){return <section className="card"><p className="eyebrow">Workouts</p><h2>Your first workout is waiting.</h2><p>No workout history yet. Browse the library, start a session, and completed workouts will appear only after you log them.</p><div className="grid two">{workoutTemplates.slice(0,6).map(w=><a className="card" key={w.id} href={`/workouts/${w.id}`}><h3>{w.title}</h3><p>{w.level} • {w.durationMinutes} min</p></a>)}</div><a className="primary" href="/workouts/library">Browse workout library</a></section>}
