import { useState } from "react";import { findWorkoutTemplate } from "../../data/workouts/workoutTemplates";
export function WorkoutPlayer({id}:{id:string}){const w=findWorkoutTemplate(id);const [done,setDone]=useState<Record<string,boolean>>({});if(!w)return <WorkoutMissing/>;return <section className="card"><p className="eyebrow">Workout player</p><h2>{w.title}</h2>{w.exercises.map((e,i)=><article className="card" key={e.id}><h3>{i+1}. {e.name}</h3><label>Sets logged <input type="number" min="0" placeholder={`${e.sets}`} /></label><label>Reps <input placeholder={e.reps ?? e.time ?? "duration"} /></label><label>Weight <input placeholder="Not entered" /></label><label>Notes <textarea placeholder="Add notes" /></label><button onClick={()=>setDone({...done,[e.id]:!done[e.id]})}>{done[e.id]?"Completed":"Mark complete"}</button><button>Substitute exercise</button><button>Add exercise</button></article>)}<button className="primary">Complete workout</button><p>No completion is saved until you complete the workout.</p></section>}
function WorkoutMissing(){return <section className="card"><h2>Workout not found</h2></section>}
